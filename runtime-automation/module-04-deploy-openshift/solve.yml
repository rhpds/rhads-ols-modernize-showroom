---
- name: Module 04 Solve — Deploy to OpenShift
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    student_user: "{{ user | default('') }}"
    devworkspace_namespace: "{{ student_user }}-devworkspace"
    coolstore_namespace: "{{ student_user }}-coolstore"
    openshift_api: "{{ openshift_api_url | default('') }}"
    student_password: "{{ password | default('') }}"
  tasks:
    # -------------------------------------------------------
    # Task 1 — Find DevSpaces workspace pod
    # -------------------------------------------------------
    - name: Find DevSpaces workspace pod
      kubernetes.core.k8s_info:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        kind: Pod
        namespace: "{{ devworkspace_namespace }}"
      register: r_workspace_pods
      ignore_errors: true

    - name: Set workspace pod name
      ansible.builtin.set_fact:
        workspace_pod: "{{ (r_workspace_pods.resources | default([]) | selectattr('status.phase', 'equalto', 'Running') | list | first).metadata.name }}"
      when: r_workspace_pods.resources | default([]) | selectattr('status.phase', 'equalto', 'Running') | list | length > 0
      ignore_errors: true

    # -------------------------------------------------------
    # Task 2 — Switch to module4 branch + login to OCP
    # -------------------------------------------------------
    - name: Stash and checkout module4
      kubernetes.core.k8s_exec:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        namespace: "{{ devworkspace_namespace }}"
        pod: "{{ workspace_pod }}"
        command: "sh -c 'cd /projects/kai-coolstore && git stash 2>/dev/null; git checkout module4 && echo checked-out-module4'"
      register: r_git_checkout
      when: workspace_pod is defined
      ignore_errors: true

    - name: Login to OpenShift + switch to coolstore project
      kubernetes.core.k8s_exec:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        namespace: "{{ devworkspace_namespace }}"
        pod: "{{ workspace_pod }}"
        command: "sh -c 'oc login --server={{ openshift_api }} --username={{ student_user }} --password={{ student_password }} --insecure-skip-tls-verify=true && oc project {{ coolstore_namespace }} && echo logged-in'"
      register: r_oc_login
      when: workspace_pod is defined
      ignore_errors: true

    # -------------------------------------------------------
    # Task 3 — Deploy PostgreSQL (lab instruction: oc new-app)
    # -------------------------------------------------------
    - name: Deploy PostgreSQL database
      kubernetes.core.k8s_exec:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        namespace: "{{ devworkspace_namespace }}"
        pod: "{{ workspace_pod }}"
        command: "sh -c 'oc new-app -n {{ coolstore_namespace }} -e POSTGRESQL_USER=quarkus -e POSTGRESQL_PASSWORD=quarkus -e POSTGRESQL_DATABASE=coolstore openshift/postgresql:latest --name=coolstore-database 2>&1 | tail -3 || echo already-exists'"
      register: r_deploy_postgres
      when:
        - workspace_pod is defined
        - r_oc_login is not failed
      ignore_errors: true

    # -------------------------------------------------------
    # Task 4 — Run Maven deploy using setsid (truly detached from exec session)
    #   setsid creates a new session — process survives when exec connection closes.
    #   nohup alone is insufficient; setsid is required.
    # -------------------------------------------------------
    - name: Start maven deploy with setsid (truly detached)
      kubernetes.core.k8s_exec:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        namespace: "{{ devworkspace_namespace }}"
        pod: "{{ workspace_pod }}"
        command: "sh -c 'cd /projects/kai-coolstore && setsid sh -c \"mvn clean compile package -DskipTests -Dquarkus.kubernetes.deploy=true > /tmp/coolstore-deploy.log 2>&1\" & echo MAVEN_STARTED_PID=$!'"
      register: r_mvn_start
      when:
        - workspace_pod is defined
        - r_git_checkout is not failed
      ignore_errors: true

    # -------------------------------------------------------
    # Task 5 — Poll for coolstore Deployment to appear (maven creates it)
    #   Then wait for OCP Build to complete
    # -------------------------------------------------------
    - name: Wait for OCP Build to appear (maven triggers S2I build)
      kubernetes.core.k8s_info:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        api_version: build.openshift.io/v1
        kind: Build
        namespace: "{{ coolstore_namespace }}"
      register: r_builds
      until: >
        r_builds.resources | default([]) |
        selectattr('status.phase', 'in', ['Running', 'Pending', 'New']) |
        list | length > 0
      retries: 18
      delay: 10
      ignore_errors: true

    - name: Wait for OCP Build to complete (up to 10 minutes)
      kubernetes.core.k8s_info:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        api_version: build.openshift.io/v1
        kind: Build
        namespace: "{{ coolstore_namespace }}"
      register: r_build_done
      until: >
        r_build_done.resources | default([]) |
        selectattr('status.phase', 'equalto', 'Complete') |
        list | length > 0
      retries: 30
      delay: 20
      ignore_errors: true

    # -------------------------------------------------------
    # Task 6 — Create route (lab says: oc get route coolstore)
    # -------------------------------------------------------
    - name: Create edge route named coolstore
      kubernetes.core.k8s_exec:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        namespace: "{{ devworkspace_namespace }}"
        pod: "{{ workspace_pod }}"
        command: "sh -c 'oc create route edge coolstore --service=coolstore --port=8080 -n {{ coolstore_namespace }} 2>/dev/null || echo route-exists'"
      when: workspace_pod is defined
      ignore_errors: true

    - name: Wait for coolstore deployment to be ready
      kubernetes.core.k8s_info:
        kubeconfig: "{{ k8s_kubeconfig | default(omit) }}"
        kind: Deployment
        name: coolstore
        namespace: "{{ coolstore_namespace }}"
      register: r_coolstore_deploy
      until: >
        (r_coolstore_deploy.resources | default([{}]) | first).status.readyReplicas | default(0) | int >= 1
      retries: 18
      delay: 10
      ignore_errors: true

    # -------------------------------------------------------
    # Report
    # -------------------------------------------------------
    - name: Report solve status
      ansible.builtin.debug:
        msg: |
          DevSpaces workspace: {{ workspace_pod | default('not found') }}
          Git checkout module4: {{ 'done' if (r_git_checkout is not failed) else 'failed' }}
          PostgreSQL: {{ 'deployed' if (r_deploy_postgres is not failed) else 'failed/skipped' }}
          Maven started: {{ r_mvn_start.stdout | default('not started') | trim }}
          Build phase: {{ (r_build_done.resources | default([]) | map(attribute='status.phase') | list | join(', ')) or 'pending' }}
          App ready: {{ 'yes' if ((r_coolstore_deploy.resources | default([{}]) | first).status.readyReplicas | default(0) | int >= 1) else 'not yet' }}
